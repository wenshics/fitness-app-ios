/** @type {const} */
const themeColors = {
  primary: { light: '#0D9488', dark: '#0D9488' },      // Teal 600 — fitness brand
  background: { light: '#FAFAFA', dark: '#F0F4F8' },   // Near-white / Light blue-grey
  surface: { light: '#FFFFFF', dark: '#FFFFFF' },       // White cards
  foreground: { light: '#374151', dark: '#374151' },   // Dark grey (readable, not black)
  muted: { light: '#71717A', dark: '#6B7280' },        // Zinc 500 / Grey 500
  border: { light: '#E4E4E7', dark: '#D1D5DB' },       // Zinc 200 / Grey 300
  success: { light: '#059669', dark: '#059669' },      // Emerald 600
  warning: { light: '#D97706', dark: '#D97706' },      // Amber 600
  error: { light: '#DC2626', dark: '#DC2626' },        // Red 600
};

module.exports = { themeColors };
