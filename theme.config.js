/** @type {const} */
const themeColors = {
  primary: { light: '#0D9488', dark: '#14B8A6' },      // Teal 600 / 500 — sophisticated fitness brand
  background: { light: '#FAFAFA', dark: '#0C0C0E' },   // Near-white / True dark
  surface: { light: '#FFFFFF', dark: '#18181B' },       // White cards / Zinc 900
  foreground: { light: '#18181B', dark: '#F4F4F5' },   // Zinc 900 / Zinc 100
  muted: { light: '#71717A', dark: '#A1A1AA' },        // Zinc 500 / Zinc 400
  border: { light: '#E4E4E7', dark: '#27272A' },       // Zinc 200 / Zinc 800
  success: { light: '#059669', dark: '#34D399' },      // Emerald 600 / 400
  warning: { light: '#D97706', dark: '#FBBF24' },      // Amber 600 / 400
  error: { light: '#DC2626', dark: '#F87171' },        // Red 600 / 400
};

module.exports = { themeColors };
